using NUnit.Framework;
using System.Collections.Generic;
using UnityEngine;

public class TrazoManager : MonoBehaviour
{ 
    public List<PuntoGuia> puntosTrazo;
    [SerializeField] TareaDibujar tareaDibujar;

    public bool trazoBien;
    //UILineRenderer lineaActual;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //if () comprobar si todos los puntos están bien, si lo están trazo bien 
    }
}
