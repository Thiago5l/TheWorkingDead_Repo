using UnityEngine;

public class PuntoGuia : MonoBehaviour
{
    [SerializeField] private float semejanza;
    public LineRenderer lineaActual;

    //Haz un manager que funcione para los trazos, y luego, si todos los trazos están bien en todos sus puntos que se gane la tarea 

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
        //for (int i = 0; i < lineaActual.Points.Length; i++)
        //{
        //    //float distance con semejanza + si está cerca
        //}
    }
}
